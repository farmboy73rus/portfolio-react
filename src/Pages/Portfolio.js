import React, { Component } from 'react';

class Contacts extends Component {
    render() {
        return (
            <div>
                Portfolio
            </div>
        );
    }
}

export default Contacts;